name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true
      - run: uv sync --locked --dev
      - run: uv run pytest
      - run: uv run ruff check .
      - run: uv run mypy src
      - run: uv build
      - name: Verify DataHub plugin registration
        run: |
          uv run python -c "from importlib.metadata import entry_points; plugins = {entry.name: entry for entry in entry_points(group='datahub.ingestion.source.plugins')}; assert plugins['rawtree'].load().__name__ == 'RawTreeSource'"
